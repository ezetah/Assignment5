@model IEnumerable<Assignment5.Models.ShoppingCart>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.songID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.userID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.songID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.userID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.shoppingCartID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.shoppingCartID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.shoppingCartID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
